export const initState = {
    products: [],
    productInput: ""
}

const reducer = (state, action) => {
    switch (action.type) {
        case "ADD":
            
            break;
    
        default:
            break;
    }
}

export default reducer